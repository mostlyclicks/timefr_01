<%= form_tag dealers_dealers_path, method: 'get' do %>
  <%= label_tag 'query', 'Dealer Search' %>
  <%= text_field_tag :search, {}, {type: "search", placeholder: "City, State or ZIP", value: (params[:search] if params[:search])} %>
  <%= submit_tag "GO" %>
<% end %>