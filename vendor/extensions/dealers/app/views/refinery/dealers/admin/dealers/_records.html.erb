<%= render 'refinery/admin/search_header', :url => refinery.dealers_admin_dealers_path %>
<div class='pagination_container'>
  <% if @dealers.any? %>
    <%= render 'dealers' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
