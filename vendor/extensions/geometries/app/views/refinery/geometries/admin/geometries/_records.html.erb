<%= render 'refinery/admin/search_header', :url => refinery.geometries_admin_geometries_path %>
<div class='pagination_container'>
  <% if @geometries.any? %>
    <%= render 'geometries' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
