<%= render 'refinery/admin/search_header', :url => refinery.home_features_admin_home_features_path %>
<div class='pagination_container'>
  <% if @home_features.any? %>
    <%= render 'home_features' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
