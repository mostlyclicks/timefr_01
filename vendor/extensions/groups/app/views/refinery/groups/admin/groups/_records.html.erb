<%= render 'refinery/admin/search_header', :url => refinery.groups_admin_groups_path %>
<div class='pagination_container'>
  <% if @groups.any? %>
    <%= render 'groups' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
