<%= render 'refinery/admin/search_header', :url => refinery.pedals_admin_pedals_path %>
<div class='pagination_container'>
  <% if @pedals.any? %>
    <%= render 'pedals' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
