<%= render 'refinery/admin/search_header', :url => refinery.bikes_admin_colors_path %>
<div class='pagination_container'>
  <% if @colors.any? %>
    <%= render 'colors' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
