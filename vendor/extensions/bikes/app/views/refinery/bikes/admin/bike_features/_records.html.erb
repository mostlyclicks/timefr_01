<%= render 'refinery/admin/search_header', :url => refinery.bikes_admin_bike_features_path %>
<div class='pagination_container'>
  <% if @bike_features.any? %>
    <%= render 'bike_features' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
