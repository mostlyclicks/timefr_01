<%= render 'refinery/admin/search_header', :url => refinery.bikes_admin_bikes_path %>
<div class='pagination_container'>
  <% if @bikes.any? %>
    <%= render 'bikes' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
