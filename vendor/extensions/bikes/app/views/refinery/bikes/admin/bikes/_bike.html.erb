<li class='clearfix record <%= cycle("on", "on-hover") %>' id="<%= dom_id(bike) -%>">
  <span class='title'>
    <%= bike.name %>

    <% if Refinery::I18n.frontend_locales.many? %>
      <span class='preview'>
        <% bike.translations.each do |translation| %>
          <% if translation.name.present? %>
            <%= link_to refinery_icon_tag("flags/#{translation.locale}.png", :size => '16x11'),
                         refinery.edit_bikes_admin_bike_path(bike, :switch_locale => translation.locale),
                         :class => 'locale' %>
          <% end %>
        <% end %>
      </span>
    <% end %>

  </span>
  <span class='actions'>
    
    <%= link_to refinery_icon_tag("application_go.png"), refinery.bikes_bike_path(bike),
        :title => t('.view_live_html'),
        :target => "_blank" %>
    
    <%= link_to refinery_icon_tag("application_edit.png"), refinery.edit_bikes_admin_bike_path(bike),
         :title => t('.edit') %>
    <%= link_to refinery_icon_tag("delete.png"), refinery.bikes_admin_bike_path(bike),
        :class => "cancel confirm-delete",
        :title => t('.delete'),
        :confirm => t('message', :scope => 'refinery.admin.delete', :title => bike.name),
        :method => :delete %>
  </span>
</li>
