<li class='clearfix record <%= cycle("on", "on-hover") %>' id="<%= dom_id(color) -%>">
  <span class='title'>
    <%= color.name %>

    <% if Refinery::I18n.frontend_locales.many? %>
      <span class='preview'>
        <% color.translations.each do |translation| %>
          <% if translation.name.present? %>
            <%= link_to refinery_icon_tag("flags/#{translation.locale}.png", :size => '16x11'),
                         refinery.edit_bikes_admin_color_path(color, :switch_locale => translation.locale),
                         :class => 'locale' %>
          <% end %>
        <% end %>
      </span>
    <% end %>

  </span>
  <span class='actions'>
    
    <%= link_to refinery_icon_tag("application_go.png"), refinery.bikes_color_path(color),
        :title => t('.view_live_html'),
        :target => "_blank" %>
    
    <%= link_to refinery_icon_tag("application_edit.png"), refinery.edit_bikes_admin_color_path(color),
         :title => t('.edit') %>
    <%= link_to refinery_icon_tag("delete.png"), refinery.bikes_admin_color_path(color),
        :class => "cancel confirm-delete",
        :title => t('.delete'),
        :confirm => t('message', :scope => 'refinery.admin.delete', :title => color.name),
        :method => :delete %>
  </span>
</li>
